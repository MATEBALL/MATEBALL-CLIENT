name: 📘 Chromatic UI Test

on:
  pull_request:
    branches:
      - develop

permissions:
  pull-requests: write
  issues: write

jobs:
  chromatic:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0